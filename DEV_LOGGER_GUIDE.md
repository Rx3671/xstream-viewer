# دليل استخدام Dev Logger

## نظرة عامة

تم إضافة نظام **Dev Logger** المؤقت لعرض رسائل log الأخطاء والمعلومات بشكل مرتب ونظيف أثناء الاختبارات والتطوير.

## الميزات

### 1. عرض تلقائي لجميع رسائل Console
- يلتقط تلقائياً جميع رسائل `console.log()`, `console.info()`, `console.warn()`, `console.error()`
- يعرضها في واجهة مرتبة وسهلة القراءة
- يحتفظ بآخر 100 رسالة فقط لتجنب استهلاك الذاكرة

### 2. واجهة مستخدم متقدمة
- **زر عائم**: يظهر في الزاوية السفلية اليمنى مع عداد للرسائل
- **نافذة قابلة للتصغير**: يمكن تصغيرها أو إغلاقها حسب الحاجة
- **تصفية حسب النوع**: عرض جميع الرسائل أو فلترتها حسب النوع (info, warn, error, success)
- **طابع زمني**: كل رسالة تحتوي على الوقت الدقيق
- **تمرير تلقائي**: تنتقل تلقائياً للرسالة الأحدث

### 3. أنواع الرسائل

#### Info (معلومات)
- اللون: أزرق
- الأيقونة: دائرة معلومات
- الاستخدام: معلومات عامة وتتبع العمليات

#### Warning (تحذير)
- اللون: أصفر
- الأيقونة: مثلث تحذير
- الاستخدام: تحذيرات لا تمنع التشغيل

#### Error (خطأ)
- اللون: أحمر
- الأيقونة: دائرة تنبيه
- الاستخدام: أخطاء تحتاج إلى معالجة

#### Success (نجاح)
- اللون: أخضر
- الأيقونة: علامة صح
- الاستخدام: عمليات تمت بنجاح

## طرق الاستخدام

### 1. استخدام Console العادي (يعمل تلقائياً)

```javascript
console.log("رسالة معلومات عادية");
console.info("معلومات إضافية");
console.warn("تحذير: قد يكون هناك مشكلة");
console.error("خطأ: حدث خطأ ما");
```

### 2. استخدام devLog المخصص (موصى به)

```javascript
import { devLog } from "@/components/DevLogger";

// رسالة معلومات
devLog.info("تم تحميل البيانات بنجاح");

// رسالة معلومات مع بيانات إضافية
devLog.info("استجابة API", { 
  status: 200, 
  data: response 
});

// تحذير
devLog.warn("سرعة الإنترنت منخفضة");

// خطأ
devLog.error("فشل الاتصال بالسيرفر", { 
  error: error.message 
});

// نجاح
devLog.success("تم حفظ الإعدادات بنجاح");
```

### 3. عرض البيانات المعقدة

```javascript
devLog.info("بيانات المستخدم", {
  username: "test_user",
  channels: 150,
  movies: 500,
  series: 200
});
```

سيتم عرض البيانات في قسم قابل للطي يمكن فتحه لرؤية التفاصيل بصيغة JSON منسقة.

## الأزرار والتحكم

### زر Dev Logs
- يظهر في الزاوية السفلية اليمنى
- يعرض عدد الرسائل الحالية
- اضغط عليه لفتح نافذة اللوقات

### أزرار النافذة
- **⬇️ تصغير/تكبير**: تصغير النافذة للشريط العلوي فقط
- **🗑️ مسح**: حذف جميع الرسائل
- **✖️ إغلاق**: إخفاء النافذة (يمكن فتحها مرة أخرى من الزر العائم)

### أزرار الفلترة
- **الكل**: عرض جميع الرسائل
- **INFO**: عرض رسائل المعلومات فقط
- **WARN**: عرض التحذيرات فقط
- **ERROR**: عرض الأخطاء فقط
- **SUCCESS**: عرض رسائل النجاح فقط

## أمثلة عملية

### مثال 1: تتبع تحميل البيانات

```javascript
devLog.info("بدء تحميل قوائم القنوات...");

try {
  const response = await fetchChannels();
  devLog.success("تم تحميل القنوات بنجاح", { 
    count: response.length 
  });
} catch (error) {
  devLog.error("فشل تحميل القنوات", { 
    error: error.message 
  });
}
```

### مثال 2: تتبع تشغيل الفيديو

```javascript
devLog.info("محاولة تشغيل القناة", { 
  channelId: channel.id, 
  streamUrl: channel.stream_url 
});

player.on('loadstart', () => {
  devLog.info("بدء تحميل البث...");
});

player.on('canplay', () => {
  devLog.success("البث جاهز للتشغيل");
});

player.on('error', (error) => {
  devLog.error("خطأ في تشغيل البث", { 
    error: error.message 
  });
});
```

### مثال 3: تتبع الاتصال بـ Xtream API

```javascript
devLog.info("الاتصال بـ Xtream API", {
  server: config.serverUrl,
  username: config.username
});

const result = await connectToXtream(config);

if (result.success) {
  devLog.success("تم الاتصال بنجاح", {
    channels: result.channels.length,
    movies: result.movies.length,
    series: result.series.length
  });
} else {
  devLog.error("فشل الاتصال", {
    reason: result.error
  });
}
```

## ملاحظات مهمة

### للاختبار المؤقت فقط
- هذا النظام مصمم للتطوير والاختبار فقط
- **يجب إزالته أو تعطيله قبل النشر للإنتاج**
- يستهلك موارد إضافية ويعرض معلومات حساسة

### إزالة النظام

لإزالة النظام بالكامل:

1. احذف الملف: `client/src/components/DevLogger.tsx`
2. أزل السطر من `App.tsx`:
   ```javascript
   import { DevLogger } from "./components/DevLogger";
   ```
3. أزل المكون من `App.tsx`:
   ```javascript
   <DevLogger />
   ```

### تعطيل مؤقت

لتعطيل النظام مؤقتاً دون حذفه، علّق السطر في `App.tsx`:

```javascript
{/* <DevLogger /> */}
```

## التخصيص

يمكنك تخصيص النظام من خلال تعديل ملف `DevLogger.tsx`:

- **تغيير الموقع**: عدّل `fixed bottom-4 right-4` في الـ className
- **تغيير الحجم**: عدّل `w-[600px] h-[500px]`
- **تغيير عدد الرسائل المحفوظة**: عدّل الرقم 100 في `if (logStore.length > 100)`
- **تغيير الألوان**: عدّل دالة `getLevelColor()`

## الدعم

إذا واجهت أي مشاكل أو احتجت إلى تخصيصات إضافية، تحدث مع Manus AI.
