# 📺 IPTV Smart Player - دليل المميزات المتقدمة

## نظرة عامة

**IPTV Smart Player** هو مشغل IPTV متقدم مبني باستخدام React و TypeScript و HLS.js، يوفر تجربة تشغيل بث مباشر احترافية مع مميزات متقدمة مستوحاة من أفضل مشغلات IPTV مفتوحة المصدر مثل IPTVnator و ArtPlayer.

---

## 🎯 المميزات الرئيسية

### 1. تشغيل البث المتقدم (Advanced Streaming)

#### ✅ دعم HLS/M3U8 الكامل
- تشغيل بث M3U8 و HLS بجودة عالية
- دعم البث المباشر (Live Streaming)
- دعم البث حسب الطلب (VOD)
- التوافق مع جميع المتصفحات الحديثة

#### ✅ وضع التأخير المنخفض (Low Latency Mode)
```javascript
lowLatencyMode: true
liveSyncDurationCount: 2
liveMaxLatencyDurationCount: 5
maxLiveSyncPlaybackRate: 1.5
```
- تقليل التأخير في البث المباشر إلى أقل من 3 ثوان
- مزامنة سريعة مع البث الحي
- تعويض تلقائي للتأخير

#### ✅ البث التكيفي (Adaptive Bitrate Streaming - ABR)
- تبديل تلقائي للجودة حسب سرعة الإنترنت
- خوارزميات ABR محسّنة لتقليل التقطيع
- تحسين استهلاك البيانات

---

### 2. إدارة الجودة (Quality Management)

#### ✅ اختيار الجودة التلقائي
- اختيار تلقائي للجودة المثلى حسب:
  - سرعة الإنترنت
  - حجم الشاشة
  - قدرة المعالج

#### ⭐ اختيار الجودة اليدوي (Manual Quality Selection)
```typescript
// Available qualities: 360p, 480p, 720p, 1080p, 4K
changeQuality(level: number)
```
- قائمة منسدلة لاختيار الجودة
- عرض معدل البت (Bitrate) لكل جودة
- تبديل فوري بدون انقطاع

#### ✅ تحسين تلقائي للجودة في:
- **وضع ملء الشاشة**: ترقية تلقائية لأعلى جودة
- **AirPlay/Chromecast**: فرض أعلى جودة متاحة
- **الشبكة البطيئة**: خفض تلقائي للجودة

---

### 3. مراقبة الأداء (Performance Monitoring)

#### ✅ كشف جودة الشبكة (Network Quality Detection)
```javascript
{
  effectiveType: '4g',
  downlink: 10.5,  // Mbps
  rtt: 50,         // ms
  saveData: false
}
```
- مراقبة مستمرة لسرعة الإنترنت
- التكيف التلقائي مع تغيرات الشبكة
- تنبيهات عند ضعف الاتصال

#### ✅ مراقبة صحة البافر (Buffer Health Monitoring)
- عرض نسبة امتلاء البافر (0-100%)
- تنبيهات عند انخفاض البافر
- تحسين ديناميكي لحجم البافر

#### ⭐ عرض الإحصائيات المباشرة (Live Statistics)
اضغط `I` لعرض/إخفاء الإحصائيات:
```
Resolution: 1920x1080
Codec: h264
Bandwidth: 8.5 Mbps
FPS: 30
Buffer: 8.2s
Latency: 120ms
Dropped Frames: 5/12000
```

---

### 4. التحكم المتقدم (Advanced Controls)

#### ⭐ سرعة التشغيل (Playback Speed)
```typescript
// Speeds: 0.25x, 0.5x, 0.75x, 1x, 1.25x, 1.5x, 1.75x, 2x, 3x, 4x
changePlaybackRate(rate: number)
```
- قائمة منسدلة لاختيار السرعة
- مؤشر مرئي للسرعة الحالية
- مفيد لمراجعة المباريات والأحداث

#### ⭐ اختصارات لوحة المفاتيح (Keyboard Shortcuts)

| المفتاح | الوظيفة |
|---------|---------|
| `Space` / `K` | تشغيل/إيقاف |
| `F` | ملء الشاشة |
| `M` | كتم الصوت |
| `↑` / `↓` | رفع/خفض الصوت |
| `←` / `→` | التقديم/التأخير 5 ثوان |
| `J` / `L` | التقديم/التأخير 10 ثوان |
| `I` | عرض الإحصائيات |
| `P` | Picture-in-Picture |
| `S` | لقطة شاشة |
| `,` / `.` | إطار سابق/تالي |
| `<` / `>` | تقليل/زيادة السرعة |
| `0` / `Home` | العودة للبداية |
| `End` | الانتقال للنهاية |

#### ⭐ Picture-in-Picture (PiP)
- تشغيل الفيديو في نافذة عائمة
- الاستمرار في المشاهدة أثناء التصفح
- دعم كامل لجميع المتصفحات الحديثة

#### ⭐ لقطات الشاشة (Screenshot)
```typescript
takeScreenshot()
```
- التقاط لقطة شاشة بجودة عالية
- حفظ تلقائي بصيغة PNG
- اسم ملف يحتوي على التاريخ والوقت

---

### 5. الصوت والترجمة (Audio & Subtitles)

#### ⭐ مسارات الصوت المتعددة (Multi-Audio Tracks)
```typescript
changeAudioTrack(trackId: number)
```
- دعم مسارات صوت متعددة
- قائمة منسدلة لاختيار اللغة
- تبديل فوري بدون انقطاع

#### ⭐ الترجمات (Subtitles)
```typescript
changeSubtitleTrack(trackId: number)
```
- دعم ترجمات مدمجة في البث
- إمكانية تفعيل/تعطيل الترجمة
- اختيار لغة الترجمة

---

### 6. البث إلى الأجهزة (Casting)

#### ✅ AirPlay Support
- دعم كامل لـ AirPlay
- بث مباشر إلى Apple TV
- ترقية تلقائية للجودة عند البث
- دعم ملء الشاشة على التلفزيون

#### ⭐ Chromecast Support (قيد التطوير)
- بث إلى Chromecast
- دعم Google Cast
- تحكم كامل من الهاتف/الكمبيوتر

---

### 7. التخصيص والإعدادات (Customization)

#### ✅ Custom Headers
```typescript
{
  userAgent: string,
  referer: string,
  origin: string
}
```
- تخصيص User-Agent لكل قناة
- إضافة Referer و Origin headers
- حل مشاكل CORS والحماية

#### ✅ حفظ الإعدادات (Settings Persistence)
- حفظ مستوى الصوت تلقائياً
- تذكر الجودة المفضلة
- حفظ السرعة المفضلة

#### ⭐ الوضع الليلي/النهاري (Dark/Light Mode)
- واجهة داكنة للمشاهدة الليلية
- واجهة فاتحة للمشاهدة النهارية
- تبديل سريع بين الأوضاع

---

### 8. الأداء والاستقرار (Performance & Stability)

#### ✅ تسريع GPU (GPU Acceleration)
```css
transform: translateZ(0);
willChange: 'transform';
backfaceVisibility: 'hidden';
```
- استخدام GPU لتحسين الأداء
- تشغيل سلس بدون تقطيع
- استهلاك أقل للمعالج

#### ✅ إدارة الذاكرة (Memory Management)
- تنظيف تلقائي للبافر القديم
- إدارة ذكية للذاكرة
- منع تسرب الذاكرة

#### ✅ معالجة الأخطاء (Error Handling)
```typescript
// Network Error Recovery
hls.startLoad()

// Media Error Recovery
hls.recoverMediaError()
```
- استرداد تلقائي من أخطاء الشبكة
- إصلاح تلقائي لأخطاء الوسائط
- إعادة محاولة ذكية

---

### 9. دعم Xtream Codes API

#### ✅ تكامل كامل مع Xtream Codes
- تسجيل دخول باستخدام بيانات Xtream
- جلب قوائم القنوات تلقائياً
- دعم Live TV, Movies, Series
- EPG (دليل البرامج) - قيد التطوير

#### ✅ إدارة القوائم (Playlist Management)
- استيراد قوائم M3U/M3U8
- تحديث تلقائي للقوائم
- تنظيم حسب الفئات
- نظام المفضلات

---

### 10. الواجهة والتجربة (UI/UX)

#### ✅ واجهة احترافية
- تصميم عصري وأنيق
- رسوم متحركة سلسة
- مؤشرات مرئية واضحة
- استجابة سريعة

#### ✅ التحكم التلقائي (Auto-hide Controls)
- إخفاء تلقائي للأزرار بعد 3 ثوان
- ظهور عند تحريك الماوس
- واجهة نظيفة أثناء المشاهدة

#### ✅ المؤشرات المرئية
- 🟢 مؤشر الجودة الحالية
- ⚡ مؤشر Low Latency
- 📶 مؤشر جودة الشبكة
- ⚠️ تنبيه البافر المنخفض
- 📺 مؤشر AirPlay/Cast النشط

---

## 🚀 التقنيات المستخدمة

### Frontend
- **React 18** - مكتبة واجهة المستخدم
- **TypeScript** - لغة البرمجة
- **Vite** - أداة البناء السريعة
- **TailwindCSS** - إطار التصميم
- **Radix UI** - مكونات واجهة المستخدم

### Video Streaming
- **HLS.js** - مكتبة تشغيل HLS
- **Video.js** - مشغل فيديو بديل
- **@videojs/http-streaming** - دعم HLS لـ Video.js

### Backend
- **Express.js** - خادم Node.js
- **Axios** - طلبات HTTP

---

## 📋 المميزات المخططة (Roadmap)

### المرحلة القادمة
- [ ] EPG (دليل البرامج التلفزيونية)
- [ ] TV Archive/Catchup (إعادة المشاهدة)
- [ ] Chromecast Support
- [ ] فلاتر الفيديو (Brightness, Contrast, Saturation)
- [ ] تسجيل البث (Recording)
- [ ] قوائم تشغيل متقدمة
- [ ] Continue Watching (متابعة المشاهدة)
- [ ] Mini Player Mode
- [ ] Theater Mode

### المستقبل البعيد
- [ ] دعم DASH
- [ ] دعم WebRTC
- [ ] تحليلات متقدمة
- [ ] تطبيق موبايل (React Native)
- [ ] تطبيق سطح المكتب (Electron/Tauri)

---

## 🎓 كيفية الاستخدام

### التشغيل الأساسي
```bash
# تثبيت المكتبات
pnpm install

# تشغيل وضع التطوير
pnpm dev

# بناء للإنتاج
pnpm build

# تشغيل الإنتاج
pnpm start
```

### استخدام المشغل
```tsx
import VideoPlayerEnhanced from '@/components/VideoPlayerEnhanced';

<VideoPlayerEnhanced
  src="https://example.com/stream.m3u8"
  poster="https://example.com/poster.jpg"
  userAgent="Custom User Agent"
  referer="https://example.com"
  origin="https://example.com"
  onReady={() => console.log('Player ready')}
  onError={(error) => console.error('Error:', error)}
/>
```

---

## 🔧 التكوين المتقدم

### تخصيص إعدادات HLS
يمكنك تعديل إعدادات HLS في `getOptimizedHLSConfig`:

```typescript
const hlsConfig = {
  lowLatencyMode: true,              // تفعيل Low Latency
  maxBufferLength: 20,               // حجم البافر بالثواني
  maxBufferSize: 30 * 1000 * 1000,  // حجم البافر بالبايت
  abrBandWidthFactor: 0.85,          // عامل ABR
  startLevel: -1,                    // الجودة الابتدائية (-1 = auto)
  capLevelToPlayerSize: false,       // عدم تقييد الجودة بحجم المشغل
};
```

### تخصيص الاختصارات
يمكنك تعديل اختصارات لوحة المفاتيح في `handleKeyPress`:

```typescript
case 'space':
  togglePlayPause();
  break;
case 'f':
  toggleFullscreen();
  break;
// أضف اختصاراتك الخاصة...
```

---

## 📊 مقارنة مع المشغلات الأخرى

| الميزة | IPTV Smart Player | IPTVnator | Video.js | Plyr |
|--------|------------------|-----------|----------|------|
| HLS Support | ✅ | ✅ | ✅ | ✅ |
| Low Latency | ✅ | ❌ | ⚠️ | ❌ |
| Manual Quality | ✅ | ✅ | ✅ | ✅ |
| Playback Speed | ✅ | ✅ | ✅ | ✅ |
| Keyboard Shortcuts | ✅ | ✅ | ⚠️ | ⚠️ |
| Statistics Overlay | ✅ | ❌ | ⚠️ | ❌ |
| Picture-in-Picture | ✅ | ✅ | ✅ | ✅ |
| Screenshot | ✅ | ❌ | ❌ | ❌ |
| AirPlay | ✅ | ⚠️ | ⚠️ | ⚠️ |
| Custom Headers | ✅ | ✅ | ❌ | ❌ |
| Network Monitoring | ✅ | ❌ | ❌ | ❌ |
| Buffer Health | ✅ | ❌ | ❌ | ❌ |
| Xtream Codes API | ✅ | ✅ | ❌ | ❌ |

---

## 🐛 المشاكل الشائعة وحلولها

### 1. البث لا يعمل
**الحل:**
- تحقق من صحة رابط البث
- تحقق من اتصال الإنترنت
- جرب تعطيل مانع الإعلانات
- تحقق من Console للأخطاء

### 2. التقطيع المستمر
**الحل:**
- خفض الجودة يدوياً
- تحقق من سرعة الإنترنت
- أغلق التطبيقات الأخرى
- جرب تحديث الصفحة

### 3. الصوت لا يعمل
**الحل:**
- تحقق من مستوى الصوت
- تحقق من عدم كتم الصوت
- جرب مسار صوت آخر
- تحقق من إعدادات المتصفح

### 4. AirPlay لا يعمل
**الحل:**
- تحقق من اتصال نفس الشبكة
- تحقق من تفعيل AirPlay على Apple TV
- جرب إعادة تشغيل الأجهزة
- استخدم Safari على Mac/iOS

---

## 📝 الترخيص

MIT License - يمكنك استخدام وتعديل المشروع بحرية.

---

## 🤝 المساهمة

نرحب بجميع المساهمات! يرجى:
1. Fork المشروع
2. إنشاء Branch جديد
3. Commit التغييرات
4. Push إلى Branch
5. فتح Pull Request

---

## 📞 الدعم

للدعم والاستفسارات:
- افتح Issue على GitHub
- راسلنا عبر البريد الإلكتروني
- انضم إلى مجتمعنا على Discord

---

## 🙏 شكر خاص

- **IPTVnator** - للإلهام والأفكار
- **ArtPlayer** - للمميزات المتقدمة
- **HLS.js** - لمكتبة HLS الرائعة
- **Video.js** - للمشغل البديل
- **مجتمع IPTV** - للدعم المستمر

---

**تم التحديث:** نوفمبر 2025
**الإصدار:** 1.0.0
**الحالة:** ✅ جاهز للإنتاج
