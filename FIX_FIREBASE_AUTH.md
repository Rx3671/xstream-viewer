# إصلاح خطأ Firebase Authentication

## المشكلة
```
Firebase: Error (auth/configuration-not-found)
```

هذا الخطأ يعني أن **Firebase Authentication** غير مُفعّل في مشروعك.

---

## الحل (خطوات بسيطة جداً)

### 1. افتح Firebase Console
اذهب إلى: https://console.firebase.google.com/

### 2. اختر مشروعك
انقر على مشروع **flixprime-sa**

### 3. فعّل Authentication
1. من القائمة الجانبية، اختر **Build** → **Authentication**
2. انقر على زر **Get Started** (الأزرق)
3. ستظهر لك قائمة بطرق تسجيل الدخول

### 4. فعّل Email/Password
1. ابحث عن **Email/Password** في القائمة
2. انقر عليه
3. فعّل المفتاح الأول "Email/Password" (اتركه على **Enabled**)
4. **لا تفعّل** "Email link (passwordless sign-in)"
5. انقر على **Save**

### 5. جرّب الموقع مرة أخرى
- افتح الموقع
- انقر على **Login**
- أنشئ حساب جديد
- يجب أن يعمل الآن! ✅

---

## تأكيد التفعيل

بعد التفعيل، ستجد في صفحة Authentication:
- تبويب **Users** (فارغ في البداية)
- تبويب **Sign-in method** (يظهر Email/Password كـ Enabled)

---

## ملاحظة مهمة

⚠️ **يجب أيضاً تفعيل Firestore Database:**

1. من القائمة الجانبية: **Build** → **Firestore Database**
2. انقر **Create database**
3. اختر **Start in production mode**
4. اختر موقع السيرفر (مثلاً: europe-west)
5. انقر **Enable**

بعدها اذهب إلى تبويب **Rules** والصق هذا الكود:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

انقر **Publish**

---

## الآن كل شيء جاهز! 🎉

يمكنك:
1. إنشاء حساب جديد
2. تسجيل الدخول
3. إضافة بيانات Xtream Codes
4. البدء بالمشاهدة!
